# ===========================================
# Parse-AI Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit the .env file to version control!
#
# This configuration uses Azure Managed Identity for authentication.
# No API keys or secrets are required!
#
# PREREQUISITES for Managed Identity:
# 1. Enable System-assigned or User-assigned Managed Identity on your Azure resource
# 2. Grant the identity "Cognitive Services OpenAI User" role on Azure OpenAI resources
# 3. Grant the identity "Storage Blob Data Contributor" role on Storage Account
# 
# For local development, DefaultAzureCredential will use:
# - Azure CLI credentials (az login)
# - Visual Studio Code credentials
# - Environment variables (AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_CLIENT_SECRET)

# ===========================================
# Azure OpenAI Configuration (Primary)
# Uses Managed Identity - no API key needed
# ===========================================
AZURE_OPENAI_ENDPOINT=https://your-openai-resource.openai.azure.com/
AZURE_OPENAI_API_VERSION=2025-01-01-preview
AZURE_OPENAI_DEPLOYMENT=gpt-4.1

# ===========================================
# Azure OpenAI Configuration (X-Ray Analysis)
# Uses Managed Identity - no API key needed
# ===========================================
AZURE_OPENAI_XRAY_ENDPOINT=https://your-xray-openai-resource.openai.azure.com/
AZURE_OPENAI_XRAY_API_VERSION=2024-12-01-preview
AZURE_OPENAI_XRAY_DEPLOYMENT=gpt-4o

# ===========================================
# Azure Blob Storage Configuration
# Uses Managed Identity - no connection string with keys needed
# ===========================================
AZURE_STORAGE_ACCOUNT_URL=https://yourstorageaccount.blob.core.windows.net
AZURE_STORAGE_CONTAINER_NAME=your-container-name

# ===========================================
# API Server Configuration
# ===========================================
API_HOST=0.0.0.0
API_PORT=8000
API_DEBUG=true
UPLOAD_FOLDER=uploads
MAX_CONTENT_LENGTH=16777216

# ===========================================
# Frontend Configuration (for Next.js)
# ===========================================
NEXT_PUBLIC_API_URL=http://localhost:8000
